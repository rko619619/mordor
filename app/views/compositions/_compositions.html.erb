<%@compositions.each do |composition| %>
  <h2> <%= link_to composition.title, composition_path(composition)%></h2>
    <%@categories.each do |category| %>
      <h2> <%= link_to category.name_of_category, composition_path(composition)%></h2>
  <% end %>
<% end %>
